import styled from 'styled-components';

export const LoginContainer = styled.div`
  h1 {
    margin-bottom: 2rem;
  }

  & .user-input {
    margin: 4rem 0;
  }

  & .state-toggle {
    font-size: 1.5rem;
    margin: 1rem 0;
  }
`;
